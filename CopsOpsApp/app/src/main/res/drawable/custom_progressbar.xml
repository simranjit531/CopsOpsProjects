<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="8dp"/>
            <gradient
                android:startColor="@color/smallgrey"
                android:centerColor="@color/smallgrey"
                android:centerY="1.0"
                android:endColor="@color/smallgrey"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="8dp"/>
                <gradient
                    android:startColor="@color/sea_color"
                    android:centerColor="@color/dark_sea_color"
                    android:centerY="1.0"
                    android:endColor="@color/sea_color"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>